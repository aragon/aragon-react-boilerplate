name: CI

on:
  push:
    branches:
      - master
    
jobs:
  build:
    name: Build & test with "Node.js v${{ matrix.node-version }}" on "${{ matrix.os }}"  

    strategy:
      fail-fast: false # don't cancel all jobs if some of them failed 
      matrix:
        node-version: [
          # 13,
          # 12,
          11,
          10
        ]
        os: [
          ubuntu-latest,
          # windows-latest,
          macOS-latest
        ]
    
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - name: Setup Node.js v${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies
        run: npm install
      - name: Test
        run: npm run test
      - name: Test aragon run
        - npm run start
      - name: Test aragon run with template
        - npm run start:ipfs:template
      - name: Start app server
        - npm run start:app
      - name: Test aragon run with http
        - npm run start:http
