{
  "name": "placeholder-app-name",
  "version": "0.0.1",
  "description": "placeholder-app-description",
  "scripts": {
    "postinstall": "npm run build-app",
    "build-app": "cd app && npm install && cd ..",
    "compile": "buidler compile --force",
    "start": "buidler start",
    "test": "buidler test"
  },
  "dependencies": {
    "@aragon/os": "^4.3.0"
  },
  "devDependencies": {
    "@aragon/buidler-aragon": "^0.0.1-beta.10",
    "@aragon/contract-test-helpers": "0.0.1",
    "@nomiclabs/buidler": "^1.0.2"
  }
}
